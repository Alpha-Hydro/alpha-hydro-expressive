{% extends '@layout/default.html.twig' %}
{% set pageHeader = page.title | default('Гидроаппаратура Wandfluh') %}

{% block title %}
    {{ pageHeader }}
{% endblock %}

{% block pageHeader %}
    {{ include('@layout/components/_pageHeader.twig') }}
{% endblock %}

{% block sidebar %}
    {{ include('@wandfluh/components/_sidebar.twig') }}
{% endblock %}

{% block content %}
    <div class="col-xs-12 col-md-9">
        <div class="page-content mt2">
            {% if page.image is defined and page.image is not null %}
                <img src="{{ page.image }}" alt="{{ page.title }}" class="pull-right">
            {% endif %}
            {{ page.contentHtml|default('')|raw }}
        </div>
    </div>
{% endblock %}