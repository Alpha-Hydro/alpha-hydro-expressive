{% if currentCategory.properties.count != 0 %}
    <div class="row">
        <div class="col-md-3">
            <div class="thumbnail">
                <img src="{{ asset('upload/wandfluh/img/' ~ currentCategory.image) }}" class="img-responsive"
                     alt="{{ currentCategory.name }}">
            </div>
        </div>
        <div class="col-md-9">
            <table class="table table-striped table-condensed text-size-small">
                {% for property in currentCategory.properties %}
                    <tr><td>{{ property.value }}</td></tr>
                {% endfor %}
            </table>
        </div>
    </div>
    <hr/>
{% endif %}